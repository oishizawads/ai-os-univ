# 改善実験記録

> 試した改善を記録する。効果があれば adopted.md へ、なければ rejected.md へ。

---

## テンプレート

```markdown
### 実験名

- 開始日：
- 終了日：
- 目的：
- 方法：
- 観察した効果：
- 所感：
- 次のアクション：採用 / 継続観察 / 中止
```

---

## 実験一覧

### 人間側支援 最小3案 14日パイロット
- 開始日：2026-06-21
- 終了日：2026-07-04（14日間）／判定日：2026-07-05
- 目的：Claude↔Codex 3ラウンド討論で収束した「人間側支援」の最小3案を、新規ファイルを増やさず既存テンプレ最小差分で試行し、**採用摩擦（継続率・記入の手間）**を測る。戦略効果（燃え尽き防止・判断較正）は14日では測らない（3ヶ月以上の別評価）。
- 方法（一括反映済み・2026-06-21）：
  - `ai-os/not-doing-list.md` 新規作成（戦略的拒否のみ・最大5件・理由/期限/再検討条件）
  - `life/health.md` に作業可能度（1指標）＋仮レビュー・トリガーを追加
  - `ai-os/decisions/TEMPLATE.md` ＋ `README.md` に独立した予測ログ表を追加（`DECISION_LOG.md` は正本二重化回避のため不変。実予測は次の重い判断時に月別ログへ）
- 継続条件（2026-07-05に判定）：
  - **not-doing**：実際に受任・延期判断を**1件以上変えた**
  - **コンディション**：**10/14日以上**記録し、トリガー発火後の計画変更が有用だった
  - **判断追跡**：この期間は**摩擦のみ**測る。予測精度は各予測の解決日まで評価しない
- 停止条件：
  - not-doing が14日間で受任・延期判断を一度も変えなかった → 中止
  - コンディションが記録10日未満 → 中止（摩擦過大）
  - コンディションのトリガーが未発火 → 有効性未検証、**常設に昇格しない**（記録のみ継続可）
- 観察した効果：（記入待ち・7/5判定）
- 所感：（記入待ち）
- 次のアクション：採用 / 継続観察 / 中止（7/5判定）

### metaフック削除（注入OFF）試験
- 開始日：2026-06-21
- 終了日：2026-06-28（それまで通常運用で観察）
- 目的：毎プロンプト注入される `[meta｜着手前の一拍]` を止めても、CLAUDE.md 単独でトリアージ挙動が保てるか確認する
- 方法：`~/.claude/hooks/metacognition-gate.cjs` を `ENABLED=false` で中立化（登録と Node 起動は残置＝厳密には「注入OFF試験」、global JSON は無傷で単一トグル）。実体確認＝「matcher なし・全 UserPromptSubmit で発火・プロンプト内容を読まず同4行を静的注入」、設計コメントの「1×/task」は不正確（実際は 1×/user-prompt ＝相槌にも発火）
- 判定基準（実害の定義）：**「重い・不可逆・納品タスクで深掘りせず着手した」事例を実害として記録する**（主観的な「物足りなさ」は実害に数えない）
- 観察した効果：（記入待ち・6/28判定）
- 所感：（記入待ち）
- 終了条件（2026-06-28に判定）：
  - **実害なし** → `~/.claude/settings.json` の `UserPromptSubmit` 登録を**完全削除**（無効コードと Node 起動を残さない）
  - **実害あり** → 旧4行ではなく、**分類器なしの1行版**で復帰（内容ベースの軽重判定器は作らない＝Codex 合意）

